{
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/p/:slug", "destination": "/api/render-page?slug=:slug" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
